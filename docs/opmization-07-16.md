# 场景配置优化项：
文案调整：描述修改为说明
身份不应该显示输入框，沟通风格和身份都采用标签的展示形式，身份单选，沟通风格多选，除了自带的类型外还需要支持新增（点击后添加一个能输入的标签，自动focus选中）、删除，自带的类型不能够被删除。

静默判定时长太长了，超过6秒时就应该播报；

文本调整：插问 改成 插话；插话后回到流程；
自然过渡选项时需要可自定义配置插话后回到流程的提示词。

新建或者编辑场景还需要支持能够选择一些预设的行业模板，选择行业模板后回显到当前新增或编辑的场景。

去掉开场白模板、声音风格这两个表单字段。
场景列表的状态应该和操作进行联动显示，如：已经发布的不展示发布操作

# 外呼流程优化项：
外呼流程列表还不支持名称搜索、状态筛选、更新时间排序，还不支持场景搜索过滤，没有刷新按钮。

bug：在列表编辑已发布的流程进入页面后修改流程内容后无法点击已发布按钮执行发布操作，自动保存后还是显示已发布的状态；

节点的配置选项只保留系统提示词和回复目标的其中之一，只保留回复目标这个配置项；

测试的抽屉弹窗点击关闭后好像还不会清空调试的内容，每次打开测试弹窗应该属于一次新的测试；测试还不支持变量注入/模拟，需要支持测试的变量新增、编辑，只在测试调试对话中注入，进行模拟会话测试；